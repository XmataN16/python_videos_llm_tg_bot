```markdown
# Video Analytics Telegram Bot

Telegram-бот для анализа видео-статистики через запросы на естественном языке. Принимает вопросы на русском, преобразует в безопасные SQL-запросы к PostgreSQL и возвращает числовые ответы.

---

## 🚀 Быстрый старт (без Docker)

### 1. Установка зависимостей

```bash
# Клонирование репозитория
git clone https://github.com/XmataN16/python_videos_llm_tg_bot
cd python_videos_llm_tg_bot

# Создание виртуального окружения
python -m venv .venv

# Активация окружения
# Windows:
.venv\Scripts\activate
# Linux/Mac:
source .venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

### 2. Настройка базы данных PostgreSQL

```bash
# Создание базы данных
psql -U postgres -c "CREATE DATABASE videos_analytics;"

# Применение миграции
psql -U postgres -d videos_analytics -f migrations/001_create_tables.sql
```

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# Telegram Bot Token (получить через @BotFather)
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Настройки базы данных
DB_HOST=localhost
DB_PORT=5432
DB_NAME=videos_analytics
DB_USER=postgres
DB_PASSWORD=
```

> 💡 **Как получить токен бота:**
> 1. Найдите `@BotFather` в Telegram
> 2. Отправьте `/newbot`
> 3. Следуйте инструкциям
> 4. Скопируйте токен вида `1234567890:AAHabc123def456ghi789jkl012mno345`

### 4. Загрузка данных

Поместите файл `videos.json` в папку `data/` создав папку
data в корне проекта, затем выполните:

```bash
python scripts/load_data.py
```

Вы увидите подтверждение загрузки:
```
✅ Загрузка завершена!
🎥 Успешно загружено видео: 358
📸 Успешно загружено снапшотов: 35946
```

### 5. Запуск бота

```bash
python -m src.bot
```

При успешном запуске:
```
INFO:__main__:✅ Подключено к базе данных PostgreSQL
INFO:__main__:🚀 Бот запущен и готов к работе
```

---

## 🧠 Архитектура и подход к обработке запросов

### Почему правило-базированный парсер вместо LLM?

В ТЗ указано: *"здесь есть несколько рабочих подходов, которые **не будут галлюцинировать** на таком объеме данных"*. Был выбран **гибридный подход без LLM**:

| Критерий | Правило-базированный парсер | LLM (Ollama/Mistral) |
|----------|-----------------------------|----------------------|
| **Надёжность** | ✅ 100% детерминированный | ⚠️ Риск галлюцинаций |
| **Безопасность** | ✅ Только параметризованные запросы | ⚠️ Риск инъекций при генерации SQL |
| **Скорость** | ✅ <100 мс на запрос | ⚠️ 2-5 сек на запрос |
| **Простота отладки** | ✅ Чёткие регулярки | ⚠️ Чёрный ящик |
| **Проходимость тестов** | ✅ Стабильно | ⚠️ Зависит от промпта |

### Как это работает

1. **Парсер (`src/parser.py`)**:
   - Анализирует текст через регулярные выражения
   - Определяет тип запроса по ключевым словам:
     - `всего видео` → `total_videos_count`
     - `выросли` + `просмотров` → `total_views_growth`
     - `разных видео` + `новые просмотры` → `videos_with_new_views`
   - Извлекает параметры: даты, UUID креатора, порог просмотров

2. **Безопасное выполнение (`src/database.py`)**:
   - Все запросы параметризованы через `asyncpg`
   - Даты конвертируются в `datetime` объекты
   - Никакого "сырого" SQL от пользователя

3. **Ответ**:
   - Всегда одно число (как требуется в ТЗ)
   - Без дополнительного текста

---

## 🗃️ Схема базы данных

### Таблица `videos` (итоговая статистика)

```sql
id UUID PRIMARY KEY
creator_id UUID
video_created_at TIMESTAMPTZ  -- дата публикации креатором
views_count INTEGER
likes_count INTEGER
comments_count INTEGER
reports_count INTEGER
created_at TIMESTAMPTZ         -- когда видео появилось в системе
updated_at TIMESTAMPTZ
```

### Таблица `video_snapshots` (почасовые замеры)

```sql
id UUID PRIMARY KEY
video_id UUID REFERENCES videos(id)
views_count INTEGER
likes_count INTEGER
comments_count INTEGER
reports_count INTEGER
delta_views_count INTEGER      -- прирост с прошлого замера
delta_likes_count INTEGER
delta_comments_count INTEGER
delta_reports_count INTEGER
created_at TIMESTAMPTZ          -- время замера (каждый час)
updated_at TIMESTAMPTZ
```

---

## 💬 Поддерживаемые запросы

| Тип запроса | Пример | Что считает |
|-------------|--------|-------------|
| **Общее количество** | `Сколько всего видео есть в системе?` | `COUNT(*) FROM videos` |
| **Видео креатора за период** | `Сколько видео у креатора aca1061a... вышло с 1 по 5 ноября 2025?` | `COUNT(*) WHERE creator_id = ... AND video_created_at BETWEEN ...` |
| **Видео с просмотрами > N** | `Сколько видео набрало больше 1000 просмотров?` | `COUNT(*) WHERE views_count > 1000` |
| **Суммарный прирост за дату** | `На сколько просмотров в сумме выросли все видео 28 ноября 2025?` | `SUM(delta_views_count) WHERE DATE(created_at) = '2025-11-28'` |
| **Уникальные видео с ростом** | `Сколько разных видео получали новые просмотры 27 ноября 2025?` | `COUNT(DISTINCT video_id) WHERE DATE(created_at) = '2025-11-27' AND delta_views_count > 0` |

---

## 🧪 Тестирование

### Ручная проверка
Отправьте боту в Telegram:
```
Сколько всего видео есть в системе?
Сколько видео у креатора aca1061a9d324ecf8c3fa2bb32d7be63 вышло с 1 по 5 ноября 2025?
Сколько видео набрало больше 1000 просмотров?
На сколько просмотров в сумме выросли все видео 28 ноября 2025?
Сколько разных видео получали новые просмотры 27 ноября 2025?
```

---

## 📁 Структура проекта

```
python_videos_llm_tg_bot/
├── .env.example              # Шаблон переменных окружения
├── .gitignore                # Исключения для Git
├── README.md                 # Этот файл
├── requirements.txt          # Зависимости Python
├── data/
│   └── videos.json           # Исходные данные (не коммитится)
├── migrations/
│   └── 001_create_tables.sql # Схема БД
├── scripts/
│   └── load_data.py          # Скрипт загрузки данных
└── src/
    ├── __init__.py
    ├── bot.py                # Основной файл бота
    ├── bot_test.py           # Можно запустить для проверки работоспособности бота
    ├── database.py           # Работа с PostgreSQL
    ├── parser.py             # Парсер запросов на русском
    └── schemas.py            # Модели данных (Pydantic)
```

---

> **Возможные улучшения или пути развития**:  
> Решение использует **гибридный подход без LLM** для максимальной надёжности и соответствия требованию *"не будут галлюцинировать на таком объёме данных"*. При необходимости интеграция с LLM (Ollama/Mistral) может быть добавлена в будущем через замену модуля `parser.py`.
```